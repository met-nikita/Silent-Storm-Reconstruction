#ifndef __A5_I_MULTIPLAYERMENU_H_
#define __A5_I_MULTIPLAYERMENU_H_
#if _MSC_VER > 1000
#pragma once
#endif // _MSC_VER > 1000
////////////////////////////////////////////////////////////////////////////////////////////////////
#include "iMain.h"
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace NGame
{
////////////////////////////////////////////////////////////////////////////////////////////////////
// CICMultiPlayer -- the queued main-loop command that opens the MULTIPLAYER setup screen. It mirrors
// the other menu-opening commands (CICCharGen / CICInGameMenu): it carries no state of its own
// (release sizeof == 12 == the bare CInterfaceCommand base); Exec() builds a CMultiPlayerInterface
// front-end and pushes it onto the global interface stack. Posted by the (separately registered)
// "multiplayer" console command; nothing in this tree posts it yet (parity-neutral surface).
////////////////////////////////////////////////////////////////////////////////////////////////////
class CICMultiPlayer: public NMainLoop::CInterfaceCommand
{
	OBJECT_BASIC_METHODS(CICMultiPlayer);
public:
	CICMultiPlayer() {}

	virtual void Exec();
};
////////////////////////////////////////////////////////////////////////////////////////////////////
} // NAMESPACE
////////////////////////////////////////////////////////////////////////////////////////////////////
#endif
