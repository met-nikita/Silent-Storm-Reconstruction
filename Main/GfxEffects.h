#ifndef __GDXEFFECTS_H_
#define __GDXEFFECTS_H_
#if _MSC_VER > 1000
#pragma once
#endif // _MSC_VER > 1000
////////////////////////////////////////////////////////////////////////////////////////////////////
namespace NGfx
{
class CGeometry;
class CTexture;
class CCubeTexture;
class CRenderContext;
//
const float F_FOG_HEIGHT = 25;
const float F_FOG_DISTANCE = 100;
inline void InitRadius( CVec4 *p, float f ) { p->x = f; p->y = f * f; p->z = 1 / f; p->w = p->z * p->z; }
inline void GetTexMapFromProjection( SHMatrix *p, int nResolution )
{
	p->x = p->x * 0.5f + p->w * ( 0.5f + 0.5f / nResolution );
	p->y = p->y * (-0.5f) + p->w * ( 0.5f + 0.5f / nResolution );
}
////////////////////////////////////////////////////////////////////////////////////////////////////
// EFFECTS
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffect
{
	virtual void Use( CRenderContext *p ) = 0;
};
////////////////////////////////////////////////////////////////////////////////////////////////////
//! write color
struct SEffConstLight : public SEffect
{
	CVec4 color;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffTexture : public SEffect
{
	CTexture *pTex;
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffTnLParticles : public SEffect
{
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffTnLTexture : public SEffect
{
	CTexture *pTex;
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
//! texture
struct SEffColoredTexture: public SEffect
{
	CTexture *pTex;
	CVec4 vColor;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffFoggedParticle: public SEffect
{
	CTexture *pFog, *pTex;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffGlow: public SEffect
{
	float fScale;
	CVec4 vColor;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffPureGeometry: public SEffect
{
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffTextureAndMirror: public SEffect
{
	CTexture *pTex;
	CCubeTexture *pTexMirror;
	float fMetalMirror, fDielMirror;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffFoggedTextureAndMirror: public SEffect
{
	CTexture *pTex, *pFog;
	CCubeTexture *pTexMirror;
	float fMetalMirror, fDielMirror;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffRegister: public SEffect
{
	CTexture *pTex;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffTransparentParticles : public SEffect
{
	CTexture *pLight;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffFoggedTransparentParticles : public SEffect
{
	CTexture *pLight, *pFog;
	//
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
struct SEffRenderCubemap : public SEffect
{
	CCubeTexture *pTex;
	virtual void Use( CRenderContext *p );
};
////////////////////////////////////////////////////////////////////////////////////////////////////
CVec4 CalcSpecularRo( float fRo );
////////////////////////////////////////////////////////////////////////////////////////////////////
} // namespace
////////////////////////////////////////////////////////////////////////////////////////////////////
#endif